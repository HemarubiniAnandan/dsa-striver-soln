# Cheat Sheet: LCM And GCD

- **Core Pattern:** Use Euclidean algorithm via math.gcd to find GCD. LCM = (A * B) // GCD.
- **Time Complexity:** O(log(min(A, B)))
- **Space Complexity:** O(1)
- **Difficulty:** Easy
